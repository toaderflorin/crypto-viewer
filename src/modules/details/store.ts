import { Dispatch } from 'react'

export type DetailsState = {}

export type SearchAction = any

export function detailsReducer(state: DetailsState, action: SearchAction) {
  switch (action.type) {
    default: {
      return state
    }
  }
}
