export type HomeState = {}

export type HomeAction = any

export const initialHomeState = {}

export function homeReducer(state: HomeState, action: HomeAction) {
  switch (action.type) {
    default: {
      return state
    }
  }
}